import Link from "next/link";

export default function SideBar() {
  return (
    <aside className="border-r border-border w-60 fixed h-full overflow-scroll no-scrollbar bg-bg-primary">
      SIDEBAR
    </aside>
  );
}
